ktor {
    deployment {
        prod {
            host = "raspberrypi.host.com"
            port = 8080
            db {
                host = "raspberrypi.host.com"
                port = "3306"
                user = "root"
                pass = "toor"
                name = "ducket-prod"
            }
        }
        dev {
            host = "localhost"
            port = 8081
            db {
                host = "localhost"
                port = "3306"
                user = "root"
                pass = "toor"
                name = "budgery"
            }
        }
    }
    application {
        modules = [ io.ducket.api ]
    }
}
