ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ io.ducket.api.ApplicationKt.module ]
    }
    jwt {
        secret = ${?JWT_SECRET}
        issuer = "io.ducket.api"
    }
}
