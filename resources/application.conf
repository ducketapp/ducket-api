ktor {
    deployment {
        host = "0.0.0.0"
        host = ${?APP_HOST}
        port = 8090
        port = ${?APP_PORT}
    }

    database {
        name = ${?DB_NAME}
        host = ${?DB_HOST}
        port = 3306
        port = ${?DB_PORT}
        driver = "com.mysql.cj.jdbc.Driver"
        user = ${?DB_USER}
        password = ${?DB_PASSWORD}
    }

    jwt {
        secret = ${?APP_SECRET}
        audience = "ducket application"
    }

    application {
        modules = [ io.ducket.api.ApplicationKt.module ]
    }
}